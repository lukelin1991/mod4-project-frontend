// size of grid
const TILE_HEIGHT = 15; // vertical
const TILE_WIDTH = 20; // horizontal
export const MAP_DIMENSIONS = [TILE_WIDTH, TILE_HEIGHT];

// size of sprites/tiles (so I dont have to set to 40px manually)
export const IMG_SIZE = 40;

// size of inventory
export const MAX_ITEMS = 8;

// make used for making animation
export const ANIMATION_SPEED = 350

// map size (testing)
export const MAP_HEIGHT = IMG_SIZE * TILE_HEIGHT;
export const MAP_WIDTH = IMG_SIZE * TILE_WIDTH;

// depending on char lvl.
export const HIGH_LV = 10;

// for varying screen sizes.
export const SCREEN_SMALL_WIDTH = 410;
export const SCREEN_SMALL_HEIGHT = 410;
export const SCREEN_MEDIUM_WIDTH = 600;
export const SCREEN_MEDIUM_HEIGHT = 680;